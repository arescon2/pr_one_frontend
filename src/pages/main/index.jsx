import { useState } from 'react';

import Generator from '../../features/generator';

const Page = (props) => {
  const { data, setData } = useState({});

  return (
    <div>
      Main page
    </div>
  )
}

export default Page;
